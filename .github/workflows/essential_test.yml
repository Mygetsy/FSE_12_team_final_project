name: test

on:
  push
  
jobs:
  build:
      runs-on: ubuntu-latest
      steps:
      - name : Git clone my repo
        uses: actions/checkout@v1
          
      - name : show dir
        run: ls -la
